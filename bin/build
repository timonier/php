#!/bin/sh
set -e -u -x
cd "$(dirname "$0")"/../src

docker build --file cli/Dockerfile --tag "${IMAGE:-timonier/php}":"${TAG:-cli}" cli

docker build --file fpm/Dockerfile --tag "${IMAGE:-timonier/php}":"${TAG:-fpm}" fpm
